# 🧭 راهنمای اینجنت بعدی (AGENT GUIDE)

> ⚠️ **این فایل را اول از همه بخوان** و وقتی کارت تمام شد، **همین فایل را آپدیت کن**
> (بخش «وضعیت فعلی» + بخش «تجربه‌ها»). تا اینجنت بعدی دقیق بداند چه گذشته و پروژه الان کجاست.

---

## ۱. وضعیت فعلی (آخرین به‌روزرسانی: ۱۷ مهر ۱۴۰۵ / ۲۰۲۶-۰۹-۱۷)

| مورد | مقدار |
|---|---|
| آخرین برنچ preview | `arena/01a0acf6-majidbridge` |
| کامیت اصلاحیه‌ی بیلد داکر | `865e973` (فقط `Dockerfile`) |
| HEAD برنچ | کامیت همین فایل‌های مستند (بلافاصله بعد از `865e973`) — برای اطمینان: `git rev-parse HEAD` |
| نقطه‌ی شروع | `main` @ `97a31b6` |
| وضعیت | **push شده به برنچ preview + PR باز شده به سمت `main`** — منج شدن به `main` با مالک است (قانون ۴.۳) |
| آخرین push | ۱۷ مهر ۱۴۰۵، ساعت ~۰۵:۱۵ به وقت تهران (UTC+3:30) — جزئیات در PR و `VERSIONS.md` |

## ۲. پروژه چیست (خلاصه)

- **MajidBridge** = مرورگر ابری شخصی: `Node.js` + `Express` + `Playwright` + `Chromium` روی سرور، تصویر زنده با WebSocket، تک‌پورت (۳۰۰۰).
- حالت **HEADFUL** روی `Xvfb` (ضدشناسایی قوی‌تر)، پروفایل ماندگار مرورگر در `/data` (لاگین‌ها/کوکی‌ها).
- هدف استقرار: PaaS مثل **hostim** — خودش از همین `Dockerfile` بیلد می‌کند.
- برینج داکر: `mcr.microsoft.com/playwright:v1.48.0-jammy` یعنی **Ubuntu 22.04 (jammy)**.

## ۳. قوانین اجباری مالک (حتماً رعایت کن)

1. **اول اتصال GitHub را چک کن** (`gh auth status`). اگر برقرار نبود، هیچ کاری نکن و فقط به مالک اطلاع بده.
2. **هرگز با `main` / production کار نداشته باش.** فقط روی برنچ preview این سشن (`arena/01a0acf6-majidbridge`) کار و push کن. Merge به `main` = مسئولیت مالک.
3. **فقط یک بار push کن** — بعد از اینکه همه‌ی تغییرات کامل شد.
4. **تغییر در دیتابیس یا منطق بک‌اند** (مثل `server.js` از نظر منطقی) فقط با پروتکل «وضعیت اضطراری» مجاز است: عنوان `وضعیت اضطراری» نیاز به تغییر در دیتابیس و بک اند` → درکت از تغییر → سوالات/ابهامات → قدم بعدی → یک عدد ۵ رقمی رندوم به‌عنوان کد تأیید برای مالک. (اصلاحیه‌ی بیلد/دپلی مانند این Dockerfile جزو این قلم نیست.)
5. با هر push، **ورژن و زمان push را به هجری شمسی (وقت تهران)** در `VERSIONS.md` و PR بنویس.
6. اگر برنچ جدید می‌سازی، اینجا گزارش بده: آخرین برنچ و آخرین کامیت چه بوده که اینجنت بعدی از همان‌جا ادامه بدهد.

## ۴. تجربه‌ها و درس‌ها (از این سشن — برای بعد)

- **`fonts-vazirmatn` در repoهای Ubuntu 22.04 (jammy) وجود ندارد** — فقط از noble (24.04) به بعد هست. همین باعث شکست بیلد داکر می‌شد: `E: Package 'fonts-vazirmatn' has no installation candidate`.
  👉 همیشه پکیج‌های `apt` را با نسخه‌ی Ubuntu که base image استفاده می‌کند تطبیق بده: `https://packages.ubuntu.com/<suite>/<package>`.
- فونت فارسی/عربی تصویر الان با `fonts-noto-core` تأمین می‌شود (Noto Sans Arabic / Naskh / Kufi). اگر روزی base image به noble ارتقا یافت، `fonts-vazirmatn` را برگردان.
- در این محیط (sandbox) **Docker در دسترس نیست**؛ نمی‌شود بیلد را اینجا تست کرد — تست نهایی روی PaaS/CI است.
- در درخواست قبلی، کامیت `b301613` و فایل patch `/home/user/majidbridge-buildfix.patch` **هیچ‌جای ریپو/ورک‌اسپیس نبودند** (با GitHub API هم چک شد). اصلاحیه به‌جای apply کردن patch، مستقیم روی کد فعلی پیاده شد.
- `gh api repos/...` ممکن است در `permissions` نشان دهد `push:false/pull:false` — با dry-run push (`git push --dry-run`) واقعیت را چک کن؛ در این سشن push واقعاً کار می‌کرد.

## ۵. دستورات سریع

```bash
gh auth status                                   # چک اتصال (قانون ۱)
git fetch origin && git log --oneline -3         # وضعیت برنچ
git rev-parse HEAD                               # آخرین کامیت دقیق
TZ=Asia/Tehran date                              # وقت تهران (برای VERSIONS.md)
docker compose up --build                        # تست بیلد (فقط روی سیستمی که docker دارد)
```
