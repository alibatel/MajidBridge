{
  "name": "majidbridge",
  "version": "2.0.0",
  "description": "Personal cloud browser v2 — Chromium on server with stealth anti-detection, HEADFUL/Xvfb mode, history/bookmarks/settings tabs, two-way clipboard, persistent profile, zoom and live viewport switching",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "start:headful": "HEADFUL=true node server.js",
    "test": "node test/e2e.mjs",
    "test:mock": "MOCK_BROWSER=true ALLOW_EVAL=true PORT=3111 node server.js & sleep 2; TEST_URL=http://127.0.0.1:3111 node test/e2e.mjs; kill %1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "1.48.0",
    "ws": "^8.18.0"
  }
}
